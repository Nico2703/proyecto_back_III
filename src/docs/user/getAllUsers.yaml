paths:
  /api/users:
    get:
      summary: Obtener todos los usuarios
      description: Obtiene todos los usuarios existentes en el sistema
      tags:
        - Users
      responses:
        200:
          application/json:
            schema:
              type: array   